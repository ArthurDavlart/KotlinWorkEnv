package org.example

import java.lang.Thread.ofVirtual
import java.util.*

class MyService {
    fun foo(): Int {
        listOf(2, 2, 3, 4, 5)
        mutableListOf(2, 3, 4).sort()

        return 2 + 2;
    }
}